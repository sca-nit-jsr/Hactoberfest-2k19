#include<stdio.h>
#include<string.h>

  int main()
  {
      char a[50],b[50];
      int i;
      printf("enter a string::");
      scanf("%s",a);
      i=0;
      while(a[i]!='\0')
      {
       b[i]=a[i];
       i++;
      }
      b[i]='\0';
      printf("%s",b);
      return 0;
  }
